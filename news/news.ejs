<ul class="list">
    <% for (const item of items) { %>
      <li <%= metadataAttrs(item) %> style="margin-bottom:5px">
        <span style="font-weight:bolder;font-size: smaller;"><%= item['date'] %>.</span>
        <span><%= item['name'] %></span>
      </li>
    <% } %>
    </ul>